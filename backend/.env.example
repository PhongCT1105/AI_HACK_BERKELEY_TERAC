# Copy to .env and fill in. Never commit the real .env.
APP_NAME="AIHackBerk API"
DEBUG=true
CORS_ORIGINS="http://localhost:3000"

# Add secrets as the project takes shape, e.g.:
# ANTHROPIC_API_KEY=
# DATABASE_URL=
